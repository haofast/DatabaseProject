Main
 - Program Start
 - User input loop
 - remove CVS logic? /put into lower level class

Schema
 - Create schema using Table class and write it
 - Read in schema/init schema file
 - alter schema?

Table/Page
 - Add layer to handle dynamic pages?
 - Read from table
    - use index for quick read?
    - limit clause?
    - allow for table to have multiple pages
 - Make page insert/indexing logic not dependent on ssn

 Report (will have to wait till after instructions)


Db schema table (program start function)
terminal user input functionality (CRUD commands, alter table?)
Insert terminal command Logic (command -> code objects)
Read terminal command (reading back from db: byte code-> code objects -> frontend display)
dynamic page functionality (B/B+ tree, code objects -> stored byte code)